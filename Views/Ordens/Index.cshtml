@model IEnumerable<SoldadosDoImperador.Models.Ordem>

@{
    ViewData["Title"] = "Registro de Ordens";
}

<h1>@ViewData["Title"]</h1>

@if (User.IsInRole("PRIMARCH"))
{
    <div class="mb-3">
        <a asp-action="Create" class="btn btn-primary">
            + Emitir Nova Ordem
        </a>
    </div>
}

<table class="table table-striped table-dark">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrazoFinal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soldado)
            </th>
            @if (User.IsInRole("PRIMARCH"))
            {
                <th style="width: 200px;">Ações</th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Titulo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PrazoFinal)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Soldado.Nome)
                </td>
                @if (User.IsInRole("PRIMARCH"))
                {
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-secondary">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Detalhes</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Excluir</a>
                    </td>
                }
            </tr>
        }
    </tbody>
</table>